{
  "name": "ukrtube-extension",
  "version": "1.3.0",
  "private": true,
  "description": "UkrTube adds a filtered Ukrainian video feed to YouTube.",
  "scripts": {
    "check": "npm run lint && npm test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "node scripts/check-project.mjs && prettier --check .",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
